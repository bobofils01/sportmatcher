<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--<style name="ErrorText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/red</item>
        <item name="android:textSize">16sp</item>
    </style>-->


    <style name="HintText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="TextInputLayoutAppearance" parent="Widget.Design.TextInputLayout">
        <item name="hintTextAppearance">@style/HintText</item>
        <!--<item name="errorTextAppearance">@style/ErrorText</item>-->
        <!--<item name="android:textColor">@color/colorApp</item>-->
        <!--<item name="android:textColorHint">@color/colorApp</item>-->
        <!--<item name="colorControlNormal">@color/colorApp</item>-->
        <item name="colorControlActivated">@color/colorApp</item>
        <item name="colorControlHighlight">@color/colorApp</item>
    </style>

    <style name="HeaderBar">
        <item name="android:background">#ffff</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">#ffff</item>
    </style>

    <!-- styles.xml -->
    <style name="MainToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:fontFamily">@font/baloo</item>
    </style>
</resources>
